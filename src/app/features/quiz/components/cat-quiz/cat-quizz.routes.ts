import { Routes } from "@angular/router";
import { CatQuizComponent } from "./cat-quiz.component";

export const CAT_QUIZ_ROUTES: Routes = [
  { path: '', component: CatQuizComponent, title: 'Take the cat quiz!' }
]
